#include <stdio.h>
// area of the rectangle using defualt values
int main()
{
    int lenght = 35, breath = 65;
    int area = lenght * breath;
    printf("the area of the triangle is %d", area);
    return 0;
}