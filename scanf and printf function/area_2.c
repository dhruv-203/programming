#include <stdio.h>

// the area of the rectangle ny taking the user inputs
int main()
{
    print();
    int lenght, breath;
    printf("enter the lenght of the triangle ");
    scanf("%d", &lenght);
    printf("Enter the breath of the triangle ");
    scanf("%d", &breath);
    printf("the area of the triangle is %d ", lenght * breath);
    end();
    return 0;
}